select
    sha2(cast(customer_id as string), 256) as customer_sk,
    customer_id,
    customer_name,
    state_code,
    city,
    postcode,
    street,
    street_number,
    unit,
    region,
    district,
    longitude,
    latitude,
    ship_to_address,
    valid_from_ts,
    valid_to_ts,
    units_purchased,
    loyalty_segment
from `fp_hack`.`b2_int`.`int_customer_current`